CREATE FULLTEXT CATALOG TextsFullTextCatalog
WITH ACCENT_SENSITIVITY = OFF

CREATE FULLTEXT INDEX ON Texts(TextDescription)
KEY INDEX PK__Texts__AB2B33B35314EF43
ON TextsFullTextCatalog
WITH CHANGE_TRACKING AUTO

--

CHECKPOINT; DBCC DROPCLEANBUFFERS; -- Empty the SQL Server cache

SELECT * FROM Texts
WHERE CONTAINS(TextDescription, '123')

--

DROP FULLTEXT INDEX ON Texts
DROP FULLTEXT CATALOG TextsFullTextCatalog